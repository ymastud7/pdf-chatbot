# Google API key for embeddings and LLM access (required)
# Get from: https://ai.google.dev/
GOOGLE_API_KEY="YOUR_GOOGLE_GEMINI_API_KEY"

# RabbitMQ host (usually localhost when using docker-compose)
RABBITMQ_HOST="localhost"

# Qdrant host (usually localhost when using docker-compose)
QDRANT_HOST="localhost"